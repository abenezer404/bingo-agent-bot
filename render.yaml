services:
  - type: web
    name: bingo-agent-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: AGENT_BOT_TOKEN
        sync: false
      - key: DATABASE_URL
        sync: false